# App version. Bump these for every release you publish.
# versionCode MUST strictly increase (1 -> 2 -> 3 ...) or Android won't install it as an update.
# versionName is what the in-app updater compares against the GitHub release tag.
versionCode=2
versionName=1.1
