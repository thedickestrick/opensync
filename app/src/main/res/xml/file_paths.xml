<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- File-manager access: expose the whole filesystem + our caches through FileProvider
         so we can hand content:// URIs to other apps (open / share). -->
    <root-path name="root" path="." />
    <external-path name="external" path="." />
    <cache-path name="cache" path="." />
    <files-path name="files" path="." />
</paths>
